@PACKAGE_INIT@

# Set package as found
set(@PROJECT_NAME@_FOUND TRUE)

include(CMakeFindDependencyMacro)
find_dependency(umd)
find_dependency(fmt)
find_dependency(nlohmann_json)
find_dependency(magic_enum)

# Include the exported targets
include("${CMAKE_CURRENT_LIST_DIR}/Metalium.cmake")

list(APPEND CMAKE_MODULE_PATH "@PACKAGE_cmakeModulesDir@")
# Set the directory containing the CMake files for the project
# get_filename_component(@PROJECT_NAME@_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

message(STATUS "Found @PROJECT_NAME@ at ${@PROJECT_NAME@_CMAKE_DIR}")
message(STATUS "Found @PROJECT_NAME@ at @PACKAGE_cmakeModulesDir@")
