diff --git a/CMakeLists.txt b/CMakeLists.txt
index 059b150..5eecfd6 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -289,7 +289,7 @@ endif ()
 function(add_headers VAR)
   set(headers ${${VAR}})
   foreach (header ${ARGN})
-    set(headers ${headers} include/fmt/${header})
+    set(headers ${headers} "${CMAKE_CURRENT_SOURCE_DIR}/include/fmt/${header}")
   endforeach()
   set(${VAR} ${headers} PARENT_SCOPE)
 endfunction()
